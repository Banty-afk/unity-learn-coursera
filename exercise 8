using System;
using System.Security.Cryptography;

namespace Exercise8
{
    /// <summary>
    /// Exercise 8 solution
    /// </summary>
    internal class Program
    {
        /// <summary>
        /// Demonstrates rolling and using two dice
        /// </summary>
        /// <param name="args">command-line arguments</param>
        static void Main(string[] args)
        {
            // initialize random number generator
            RandomNumberGenerator.Initialize();

            // create two dice
            Die die = new Die();
            Die die2 = new Die();   

            die.Roll();
            die2.Roll();

            int topside1 = die1.TopSide;
            int topside2 = die2.TopSide;

            Console.WriteLine("Die 1 rolled:" + topside1);
            Console.WriteLine("Die 2 rolled:" + topside2);  

            int aum = topside1 + topside2;
            Console.WriteLine("The sum of both dice is:" + sum);





            // tell the dice to roll themselves

            // print the top sides and the sum of the dice
        }
    }
}
